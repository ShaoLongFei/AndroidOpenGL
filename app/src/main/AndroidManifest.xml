<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myopengldemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".shape.TriangleActivity" />
        <activity android:name=".shape.RegularTriangleActivity" />
        <activity android:name=".shape.TriangleColorFullActivity" />
        <activity android:name=".shape.SquareActivity" />
        <activity android:name=".shape.RotateTriangleActivity" />
        <activity android:name=".shape.OvalActvity" />
        <activity android:name=".shape.CubeActivity" />
        <activity android:name=".shape.ConeActivity" />
        <activity android:name=".shape.CylinderActivity" />
        <activity android:name=".shape.BallActivity" />
        <activity android:name=".shape.BallWithLightActivity" />
    </application>

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

</manifest>